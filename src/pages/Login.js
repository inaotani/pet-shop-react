import React from "react";
import "./../index.css";
import Layout from './../components/Layout'

const Login = () => {
    return (
        <Layout>
            a
        </Layout>
    );
};

export default Login;